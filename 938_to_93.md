• Sisanya tidak hilang; tersaring karena report terakhir memang sangat spesifik: hanya package yang memiliki anti-
  pattern, punya runtime evidence, berhasil dimapping ke job history melalui Execution ID, dan job-nya aktif.

  Rekonsiliasi jumlahnya:

   Tahap                                                                    Jumlah
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━
   Package di SSISDB catalog                                                   938
  ──────────────────────────────────────────────────────────────────  ─────────────
   DTSX berhasil diekstrak                                                     781
  ──────────────────────────────────────────────────────────────────  ─────────────
   DTSX yang memiliki anti-pattern finding                                     409
  ──────────────────────────────────────────────────────────────────  ─────────────
   Package yang memiliki runtime evidence periode scan                 sekitar 200
  ──────────────────────────────────────────────────────────────────  ─────────────
   Unique package anti-pattern yang berhasil dimapping ke job aktif             91
  ──────────────────────────────────────────────────────────────────  ─────────────
   Baris report JOB–STEP–PACKAGE                                                93

  Perbedaan 91 vs 93 terjadi karena ada package yang muncul pada lebih dari satu job/step aktif.

  Penyaringan utamanya:

  938 catalog packages
   → 781 DTSX tersedia secara statik
   → 409 DTSX memiliki anti-pattern
   → hanya yang punya execution/job history matching
   → hanya job_enabled = 1
   → 93 JOB–STEP–PACKAGE rows

  Ada juga perbedaan populasi antara catalog dan hasil ekstraksi:

  - 357 package catalog tidak memiliki kecocokan langsung di static extraction.
  - Ini bukan berarti package pasti tidak ada; kemungkinan berasal dari perbedaan snapshot, project/package duplikat,
    atau package yang tidak ikut diekstrak.

  - File anti-pattern hanya mencakup 409 DTSX, bukan seluruh 938 catalog package.

  Jadi report saat ini adalah report prioritas workload aktif, bukan inventory lengkap 938 package.

  Untuk membuat report seluruh 938, perlu report terpisah yang memasukkan:

  - package tanpa anti-pattern,
  - package tanpa runtime execution,
  - package tanpa job mapping,
  - job disabled,
  - dan 357 package yang belum punya static DTSX matching.

  Sedangkan untuk tujuan menentukan prioritas tuning, 93 baris tersebut adalah subset yang paling actionable.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

─ Conversation recap ───────────────────────────────────────────────────────────────────────────────────────────────────

  Tujuannya merekonsiliasi 93 DTSX dalam list dengan 938 pada report. Ditemukan sisanya tersaring oleh kriteria anti-
  pattern, runtime evidence, pemetaan Execution ID, dan job aktif; langkah berikutnya menentukan apakah perlu report
  tanpa filter tersebut.